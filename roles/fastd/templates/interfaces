allow-hotplug {{ fastd_interface_name }}
iface {{ fastd_interface_name }} inet manual
	up ip link set up $IFACE
	up batctl -m {{ batman_interface_name }} if add $IFACE
	down batctl -m {{ batman_interface_name }} if del $IFACE
	down ip link set down $IFACE
