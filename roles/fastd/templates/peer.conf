key "{{ item.value.key }}";
{# Insert peers with matching port number #}
{% for remote in item.value.remotes %}
{% if remote.endswith(" port %s" % fastd_port) %}
remote {{ remote }};
{% endif %}
{% endfor %}
float yes;
