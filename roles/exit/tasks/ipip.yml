---
- name: Install IPIP interfaces file
  template: >
    src=ipip/interface
    dest=/etc/network/interfaces.d/{{ exit_ipip_interface }}

- name: Set interfaces up
  command: ifup {{ exit_ipip_interface }}
  register: ifup_result
  changed_when: '"already configured" not in ifup_result.stderr'
