---
- name: Install hook
  template: >
    src=default/up.sh
    dest=/etc/network/if-up.d/{{ffhb_routing_table}}
    mode=0755
  when: exit == "default"

- name: Remove hook
  file: path=/etc/network/if-up.d/{{ffhb_routing_table}} state=absent
  when: exit != "default"
