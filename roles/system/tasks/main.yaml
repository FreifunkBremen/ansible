---
- name: Setting hostname
  hostname: name={{ hostname }}
  when: hostname != 'defaulthost'

- name: Reboot host
  command: reboot
  when: reboot-flag

- name: Installing sysctl.d confs
  copy: src=sysctl.d/ dest=/etc/sysctl.d/
  notify: reload sysctl
