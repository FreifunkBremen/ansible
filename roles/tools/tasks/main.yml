---
- name: Install a bunch of helpful tools
  apt: name={{item}}
  with_items:
    - ca-certificates
    - curl
    - less
    - molly-guard
    - tcpdump
    - tmux

- name: Remove useless tools
  apt: name={{item}} state=absent
  with_items:
    - screen
