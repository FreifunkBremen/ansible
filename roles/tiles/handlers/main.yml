---
- name: reload apache
  service: name=apache2 state=reloaded

- name: restart tessera
  command: svc -t /home/{{ tiles_user }}/.config/etc/run-tessera

- name: restart tessera log
  command: svc -t /home/{{ tiles_user }}/.config/etc/run-tessera/log
