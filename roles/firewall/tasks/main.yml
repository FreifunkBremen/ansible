---
- name: Install packages
  apt: name=iptables-persistent

- name: Install firewall rules
  template: >
    src={{ item }}
    dest=/etc/iptables/{{ item }}
  with_items:
    - rules.v4
    - rules.v6
  notify:
    - reload iptables-v4
    - reload iptables-v6
