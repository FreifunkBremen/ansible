---
- name: Install needed tools
  apt: name=ethtool

- name: Clone respondd announce repository
  git: >
    repo={{ mesh_announce_git_root }}
    dest=/opt/{{ site_code }/mesh-announce/
  notify:
    - restart respondd

- name: Install system unit for vpn
  template: >
    src=service-vpn
    dest=/lib/systemd/system/respondd.service
  notify:
    - reload systemd
    - restart respondd
  when: respondd_vpn

- name: Install system unit for default interface
  template: >
    src=service
    dest=/lib/systemd/system/respondd.service
  notify:
    - reload systemd
    - restart respondd
  when: not respondd_vpn

- name: Enable respondd
  service: name=respondd enabled=yes

- name: Install cronjob to automatically update alfred announce
  template: >
    src=cronjob
    dest=/etc/cron.d/mesh-announce-alfred

- name: Open firewall for respondd
  template: src=firewall.sh dest={{ firewall_path }}/30-respondd
  when: firewall_enabled
  notify: reload firewall
