# {{ ansible_managed }}
[Unit]
Description=Respondd

[Service]
ExecStart=/opt/{{ site_code }}/respondd-announce/respondd.py -b {{ batman_interface }} -i {{ main_brigde }} -i vpn-{{ site_code }}-legacy -i vpn-{{ site_code }} -i vpn-{{ site_code }}-bbone -d /opt/{{ site_code }}/respondd-announce/

[Install]
WantedBy=multi-user.target
