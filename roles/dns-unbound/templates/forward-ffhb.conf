# {{ ansible_managed }}
domain-insecure: "ffhb."
forward-zone:
  name: "ffhb."
{% if "address" in ansible_default_ipv6 %}
  forward-addr: {{ ansible_default_ipv6.address }}
{% elif "address" in ansible_default_ipv4 %}
  forward-addr: {{ ansible_default_ipv4.address }}
{% endif %}
