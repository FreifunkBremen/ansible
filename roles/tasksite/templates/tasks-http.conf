# {{ ansible_managed }}

<VirtualHost *:80>
  Include /etc/apache2/{{ tasks_user }}.{{ tasks_domain }}-common.conf
</VirtualHost>

<VirtualHost *:80>
  ServerName {{ tasks_subdomain }}.{{ alt_domain }}
  RedirectPermanent / http://{{ tasks_subdomain }}.{{ main_domain }}/
</VirtualHost>
