---
- hosts: serviceservers
  vars:
    batman_gateway: true

    # for ipv4 exit
    openvpn_exit_host: 'nl.hide.me'
    openvpn_exit_username: 'test'
    openvpn_exit_password: 'pass'

    #for system
    # hostname: '{{ site_vpn_prefix }}{{ vpn_id }}'
  roles:
  # Works already comment out to save time
  #- batman-adv-14
  #- fastd
  #- alfred-announce
  #- openvpn-exit
  #- icvpn
  #- ipv6-exit
  #- ntp-chrony
  #TODO Work on process
  - firewall
  
  # Works also comment out to save time
  #- dns-unbound
  #- dhcp-dnsmasq
  #- tools-net
  #- tools-system-geno
  -- system
